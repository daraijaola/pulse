# PULSE app (frontend)

Vite + React mobile UI. Deploy root for Vercel.

```bash
npm install
npm run dev      # local
npm run build    # dist/
```

See root [README.md](../README.md) for MagicBlock path, env vars, and demo script.
